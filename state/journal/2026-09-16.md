# Diario 2026-09-16

Equity: apertura 550,00 → chiusura 550,00 (0,00%) | SPY −0,44% | modalità: **normal**

Giorno 1 del tracking. Il conto è rimasto flat tutta la sessione: **nessuna run operativa è girata oggi** (né CIO, né Trader, né Position Manager). L'unica attività registrata prima di questa run è il test di connessione delle 14:46Z. Quindi non c'è stata alcuna decisione di trading da valutare: lo 0,00% di oggi non è il risultato di una selezione, è l'assenza del processo a monte.

## ⚠ Anomalie

1. **Catena delle routine non partita.** Il piano `state/plans/2026-09-16.md` non esiste e `decisions.jsonl` era vuoto fino a questa run. Le routine CIO (08:40 ET), Trader (09:20 ET) e Position Manager (11:30 ET) non hanno lasciato traccia in `runs.csv`, che oltre al test contiene solo questa run. Non è un errore di mercato: o le cron non sono ancora attive, o sono fallite in silenzio. **Da verificare dal proprietario prima della sessione di domani**, altrimenti il sistema continua a non produrre dati e ogni metrica resta vuota.
2. **`risk-state.json` era ancora su valori teorici** (500 USD) contro i 550 reali del conto: un errore del 10% sulla base di calcolo del sizing e dei circuit breaker. Allineato in questa run (vedi sotto). Se un Trader fosse partito stamattina avrebbe dimensionato sul capitale sbagliato.
3. **Stato dei setup incoerente** tra `playbook/README.md` (`orb-sip`/`gap-go` = `in_prova`) e metriche §6 (0 trade ⇒ `sperimentale`). Dettagli e proposta in `state/memory/playbook-stats.md`. Impatta il rischio massimo per trade (1% contro 0,5%).

## Trade

| simbolo | setup | R | esito | voto processo |
|---|---|---|---|---|
| _(nessun trade, né reale né shadow)_ | – | – | – | – |

Fill del giorno: `GET /v2/account/activities/FILL?date=2026-09-16` → `[]`. Ordini chiusi: `[]`. Posizioni a fine giornata: `[]`. Ordini aperti: `[]`.

## Cosa ha funzionato / cosa no

- **Ha funzionato:** l'infrastruttura di lettura. Clock, calendario, account, posizioni, ordini, attività e barre 1 minuto SIP hanno risposto tutti correttamente; alle 16:16 ET lo storico SIP copriva l'intera sessione (391 barre da 13:30Z a 20:00Z).
- **Non ha funzionato:** la catena operativa. Un sistema che non decide non può essere valutato, e quattro giorni così azzererebbero il senso del tracking.
- Essere flat ha battuto SPY di **+0,44 punti** oggi, ma **non è un merito**: è un caso. Registrarlo come "alpha" sarebbe il primo modo per ingannarsi.
- SPY ha avuto un range intraday ampio (749,60-761,67, cioè 1,6%) chiudendo a −0,44%: una giornata che avrebbe offerto movimento ai setup intraday.
- La finestra operativa 9:30-10:30 ET è stata invece quasi piatta (**−0,05%**): l'apertura non aveva direzione, quindi anche con le routine attive un `orb-sip` o un `gap-go` avrebbero avuto poco da mordere. Il grosso del movimento è arrivato dopo.

## Decisioni di non-trade corrette o sbagliate

- **Crypto (B8), no-trade — corretta.** Nessuna posizione crypto aperta, quindi nessuno stop GTC da verificare o inviare. Non ho aperto nulla: senza tesi preparata, senza conferma dai dati live e senza il passaggio dal Risk Officer, un ingresso in una run di sola manutenzione sarebbe stato un trade senza vantaggio documentato. Registrata in `decisions.jsonl`.
- Nessun'altra decisione da valutare: non essendoci un piano, non ci sono tesi scartate da seguire in ombra (`shadow.csv` resta vuoto).

## Calibrazione del giorno

Nessuna previsione emessa (`forecasts.csv` vuoto), quindi **nessun Brier score**. Il confronto con il Brier "ingenuo" richiede uno storico di win rate che ancora non esiste. Prima misura possibile: il primo giorno con previsioni registrate.

## Equity e rischio

- Riga aggiunta a `equity.csv`: equity 550,00 invariata, drawdown 0,00%, SPY giorno −0,4410%, SPY cumulato −0,4410%, SPY 9:30-10:30 −0,0520%.
- `risk-state.json`: essendo `updated_by == "init"`, allineati `hwm`, `equity`, `last_equity` e `week_start_equity` da 500,00 teorici a **550,00** reali. Drawdown 0% ⇒ modalità **normal** (regola §5 dei circuit breaker). Nessun blocco giornaliero o settimanale attivo.
- Oggi è mercoledì: la regola del lunedì su `week_start_equity` non si applica: il valore è stato comunque fissato dall'allineamento di init.

## Lezioni candidate

_(nessuna promossa in `lessons.md`: servono ≥ 2 casi confermati)_

- **C1 — Il Coach deve verificare che le routine a monte abbiano girato, non solo i numeri.** Oggi l'assenza di dati era l'informazione più importante della giornata, e non compare in nessuna metrica: un ledger vuoto e un ledger in pareggio si assomigliano troppo. Serve un secondo caso prima di promuoverla a regola.
- **C2 — Uno stato inizializzato con valori teorici va allineato prima che qualcuno lo usi, non al primo Coach.** Un solo caso finora (oggi).
